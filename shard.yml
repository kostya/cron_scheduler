name: cron_scheduler
version: 0.4.0

authors:
  - Konstantin Makarchev

dependencies:
  cron_parser:
    github: kostya/cron_parser
    version: '>= 0.4.0'

crystal: ">= 0.35.1, < 2.0.0"

license: MIT
